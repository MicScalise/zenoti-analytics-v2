# =============================================================================
# API Test Environment — DR-007 (dotenv before imports)
# These values are safe for test environments only. NEVER use in production.
# =============================================================================
DATABASE_URL=postgresql://za:za_test@localhost:5432/za_test
REDIS_URL=redis://localhost:6379/1
NODE_ENV=test
LOG_LEVEL=warn
JWT_SECRET=test-secret-key-do-not-use-in-production
JWT_EXPIRES_IN=1h
PORT=0
